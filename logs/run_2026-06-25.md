# Routine Run — 2026-06-25

## University Scraped: Tufts University (#46)

**Source**: `as.tufts.edu/{dept}/academics/courses` (A&S departments) and subdomain sites (`chem.tufts.edu`, `math.tufts.edu`, `tdps.tufts.edu`, `cogsci.tufts.edu`, `museumstudies.tufts.edu`)

**Script**: `scripts/scrape_tufts.py`

**Output**: `data/tufts/tufts_2026.csv`, `data/tufts/tufts_summary.json`

## Results

| Metric | Value |
|--------|-------|
| Total courses | 2,187 |
| Progressive signal | 470 (21.49%) |
| Western canon signal | 192 (8.78%) |
| Climate narrow | 31 (1.42%) |
| Climate broad | 71 (3.25%) |

**By area**: Other 1,063 (49%), Humanities 522 (24%), Social Sciences 302 (14%), STEM 300 (14%)

## Notes

- Tufts was previously logged as `blocked_by_network` (the registrar URL was 404). The actual catalog lives on individual department subpages at `as.tufts.edu`.
- Three HTML/text format variants required:
  1. **Standard**: `DEPT NUM Title. Description.` (all in one `<p>`)
  2. **Number-only**: `0001 Title. Description.` (CHEM subdomain — dept implicit)
  3. **Split**: `DEPT NUM Title` header `<p>` followed by description `<p>` (MATH subdomain)
  4. **Dash-dept**: `FMS- 0001 Title . CrossRef Description` (FMS — trailing dash on dept code, cross-list before desc)
- Several departments use `/courses` URL instead of `/academics/courses`: JST, LAST, STS, ILVS.
- Departments with no course data (redirect to SIS): ILCS, ROM, RCD, MES, ENV.
- 2 departments returned 404: medieval-studies, middle-eastern-studies.

## Status After This Run

- `tufts` moved from `blocked_by_network` → `completed`
- `blocked_by_network` count: 16 (was 17)
